ul.payment-side-list
    li._item
        span._icon
            svg(role="img", focusable="false", aria-hidden="true", width="18", height="14", fill="currentColor")
                use(xlink:href="icons.svg#card")
        | Access over 750+ classes

    li._item
        span._icon
            svg(role="img", focusable="false", aria-hidden="true", width="22", height="18", fill="currentColor")
                use(xlink:href="icons.svg#cards")
        | Full access to programs

    li._item
        span._icon
            svg(role="img", focusable="false", aria-hidden="true", width="16", height="16", fill="currentColor")
                use(xlink:href="icons.svg#calendar")
        | Daily scedule suggestions
